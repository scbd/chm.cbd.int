define(["lodash", 'directives/ammap3'], function(_) { 'use strict';

    return ["$scope", "$http", "realm",
     function ($scope, $http, realm) {

        $scope.countries=[];
        $scope.colors={};



    }];
});
